version: 1
verification:
  test_command: null
  setup_command: null
  require_tests: true
  proof_tests: auto
integrity:
  deleted_tests: block
  new_skips: block
  assertion_weakening: block
  ci_changes: block
  dependency_expansion: block
  api_contract_changes: block
coverage:
  minimum_delta: 0
dependencies:
  lockfile_changes: block
network:
  mode: deny
  domains: []
security:
  isolated_runner_required: true
receipt:
  signature_required: true
